apiVersion: v1
kind: Service
metadata:
  name: paymetv-app-service
  labels:
    app: paymetv-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: paymetv-app

